import { CookPortalShell } from "@/components/cook-portal-shell"

export default function ForCooksLayout({ children }: { children: React.ReactNode }) {
  return <CookPortalShell>{children}</CookPortalShell>
}
